export class Combo {
    id: number;
    name: string;
    basePrice: number;

    constructor(id:number, name:string, basePrice:number,) {
        this.id = id;
        this.name = name;
        this.basePrice = basePrice;
    }
}